{
  "name": "streamvault-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker — IPTV proxy for Stalker, Xtream, M3U (replaces stalker-proxy)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
